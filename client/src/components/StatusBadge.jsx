export default function StatusBadge({ children, tone }) { const value = tone || String(children).toLowerCase(); const kind = value.includes('pass') || value.includes('verif') || value === 'completed' ? 'success' : value.includes('fail') || value.includes('critical') ? 'danger' : value.includes('review') || value.includes('attention') || value.includes('warning') ? 'warning' : 'info'; return <span className={`status-badge ${kind}`}><i />{children}</span> }
