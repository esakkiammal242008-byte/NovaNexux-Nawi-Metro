import { AlertTriangle, Bell, Info, ShieldAlert } from 'lucide-react'
import StatusBadge from '../components/StatusBadge'
export default function Alerts({data}) { const icons={INFO:Info,WARNING:AlertTriangle,CRITICAL:ShieldAlert}; return <div className="page"><div className="page-heading"><div><p className="eyebrow">MONITORING</p><h1>Alerts</h1><p className="muted">Signals that need an operator's attention.</p></div><div className="alert-count"><Bell size={16}/> 3 active alerts</div></div><div className="alert-cards">{data.alerts.map((a)=><div className={`alert-card ${a.level.toLowerCase()}`} key={a.text}>{(()=>{const Icon=icons[a.level];return <Icon size={22}/>})()}<div><StatusBadge>{a.level}</StatusBadge><h2>{a.text}</h2><p>{a.time}</p></div><button className="secondary-button">Acknowledge</button></div>)}</div></div> }
