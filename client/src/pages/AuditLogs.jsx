import { Activity, Download } from 'lucide-react'
import DataTable from '../components/DataTable'
export default function AuditLogs({data}) { const columns=[{key:'time',label:'Timestamp',render:r=><span className="mono">{r.time}</span>},{key:'user',label:'User'},{key:'action',label:'Action',render:r=><strong>{r.action}</strong>},{key:'entity',label:'Entity',render:r=><span className="mono">{r.entity}</span>},{key:'details',label:'Details'}]; return <div className="page"><div className="page-heading"><div><p className="eyebrow">TRACEABILITY</p><h1>Audit logs</h1><p className="muted">Immutable activity history for your local demo workspace.</p></div><button className="secondary-button"><Download size={15}/> Export log</button></div><section className="panel table-panel"><div className="table-toolbar"><div className="table-meta"><Activity size={16}/> All activity</div><span className="table-meta">Last updated moments ago</span></div><DataTable columns={columns} rows={data.audit}/></section></div> }
