import { Link } from 'react-router-dom'
import { Download, Gauge, Plus, Search } from 'lucide-react'
import DataTable from '../components/DataTable'
import StatusBadge from '../components/StatusBadge'
export default function Instruments({ data }) { const columns = [{ key: 'id', label: 'Instrument ID', render: (r) => <strong className="mono">{r.id}</strong> }, { key: 'manufacturer', label: 'Manufacturer' }, { key: 'model', label: 'Model' }, { key: 'serial', label: 'Serial number', render: (r) => <span className="mono">{r.serial}</span> }, { key: 'capacity', label: 'Capacity' }, { key: 'interval', label: 'Verification scale' }, { key: 'accuracy', label: 'Class' }, { key: 'location', label: 'Location' }, { key: 'status', label: 'Status', render: (r) => <StatusBadge>{r.status}</StatusBadge> }]; return <div className="page"><div className="page-heading"><div><p className="eyebrow">ASSET REGISTER</p><h1>Instruments</h1><p className="muted">Manage the weighing instruments in your verification workspace.</p></div><Link className="primary-button" to="/new-instrument"><Plus size={17} /> Add instrument</Link></div><section className="panel table-panel"><div className="table-toolbar"><div className="search table-search"><Search size={16} /><input placeholder="Filter instruments" /></div><button className="secondary-button"><Download size={15} /> Export register</button></div><DataTable columns={columns} rows={data.instruments} /></section><div className="footnote"><Gauge size={15} /> Instrument records are stored locally in this prototype.</div></div> }
