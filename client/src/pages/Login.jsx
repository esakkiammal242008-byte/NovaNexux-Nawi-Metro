import { useState } from 'react'
import { ArrowRight, LockKeyhole, Mail, ShieldCheck } from 'lucide-react'
export default function Login({ onLogin }) { const [email, setEmail] = useState('tester@novanexus.com'); const [password, setPassword] = useState('Tester@123'); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const submit = async (e) => { e.preventDefault(); setLoading(true); setError(''); try { await onLogin({ email, password }) } catch (err) { setError(err.response?.data?.error || (err.request ? 'Unable to connect to NovaNexus server.' : err.message)) } finally { setLoading(false) } }; return <div className="login-page"><div className="login-visual"><div className="login-grid" /><div className="login-brand"><div className="brand-mark"><ShieldCheck size={22} /></div><strong>NOVA<span>NEXUS</span></strong></div><div className="login-message"><p className="eyebrow">METROLOGY OPERATIONS PLATFORM</p><h1>Trust every<br /><em>measurement.</em></h1><p>From manual test records to secure digital reports.</p></div><div className="login-footer">OIML R 76-BASED WORKFLOW <span>•</span> PROTOTYPE ENVIRONMENT</div></div><div className="login-panel"><div className="login-form"><p className="eyebrow">WELCOME BACK</p><h2>Sign in to NovaNexus</h2><p className="muted">Access your instrument verification workspace.</p><form onSubmit={submit}><label>Email address<div className="input-icon"><Mail size={16} /><input value={email} onChange={(e) => setEmail(e.target.value)} type="email" required /></div></label><label>Password<div className="input-icon"><LockKeyhole size={16} /><input value={password} onChange={(e) => setPassword(e.target.value)} type="password" required /></div></label>{error && <p className="form-error">{error}</p>}<button className="primary-button full" type="submit" disabled={loading}>{loading ? 'Connecting...' : 'Sign in'} {!loading && <ArrowRight size={16} />}</button></form><div className="demo-accounts"><strong>Demo access</strong><span>admin@novanexus.com / Admin@123</span><span>tester@novanexus.com / Tester@123</span></div><small className="legal">Live API authentication · JWT protected workspace</small></div></div></div> }
