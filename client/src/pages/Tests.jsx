import { Link } from 'react-router-dom'
import { ClipboardCheck, Plus, Search } from 'lucide-react'
import DataTable from '../components/DataTable'
import StatusBadge from '../components/StatusBadge'
export default function Tests({ data }) { const columns = [{ key:'id',label:'Test ID',render:r=><strong className="mono">{r.id}</strong>},{key:'instrument',label:'Instrument',render:r=><span className="mono">{r.instrument}</span>},{key:'type',label:'Test type'},{key:'date',label:'Date'},{key:'tester',label:'Tester'},{key:'result',label:'Result',render:r=><StatusBadge>{r.result}</StatusBadge>},{key:'status',label:'Status',render:r=><StatusBadge>{r.status}</StatusBadge>}]; return <div className="page"><div className="page-heading"><div><p className="eyebrow">QUALITY RECORDS</p><h1>Test history</h1><p className="muted">A complete, traceable record of instrument evaluation activity.</p></div><Link className="primary-button" to="/new-test"><Plus size={17}/> New test</Link></div><section className="panel table-panel"><div className="table-toolbar"><div className="search table-search"><Search size={16}/><input placeholder="Search test ID or instrument"/></div><span className="table-meta">87 total tests</span></div><DataTable columns={columns} rows={data.tests}/></section><div className="callout"><ClipboardCheck size={17}/><div><strong>OIML R 76-based configurable evaluation</strong><span>Prototype evaluation workflow — not legal certification.</span></div></div></div> }
